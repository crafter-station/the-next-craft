---
title: Reglas
---

<div className="flex min-h-[64svh] flex-col justify-center gap-10">
  <h1 className="font-mono text-[clamp(2.5rem,7vw,5.5rem)] font-semibold uppercase tracking-[-0.07em] text-[var(--text)]">Las reglas</h1>
  <div className="grid gap-4 md:grid-cols-3">
    {[
      ['Usen Convex', 'como backend'],
      ['Pregunten', 'a los mentores'],
      ['Cuiden', 'los espacios'],
    ].map(([action, detail]) => (
      <div key={action} className="flex min-h-48 items-center justify-center bg-[var(--screen-dim)] p-6 text-center">
        <p className="font-mono text-[clamp(1.35rem,3vw,2.25rem)] font-semibold uppercase leading-tight tracking-[-0.04em] text-[var(--text)]">{action}<br /><span className="text-[var(--text-dim)]">{detail}</span></p>
      </div>
    ))}
  </div>
</div>
