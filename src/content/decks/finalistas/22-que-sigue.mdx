---
title: Qué sigue
---

<div className="flex min-h-[64svh] flex-col justify-center gap-9">
  <div className="flex flex-col gap-4">
    <h1 className="font-mono text-[clamp(2.25rem,6vw,4.5rem)] font-semibold uppercase tracking-[-0.07em] text-[var(--text)]">Qué sigue</h1>
    <p className="max-w-3xl font-mono text-sm leading-relaxed text-[var(--text-dim)] md:text-base">Los cinco pitchean ante el mismo jurado, con la misma rúbrica. Nadie compite contra su sede: compiten contra el resto de la región.</p>
  </div>

  <div className="grid gap-4 md:grid-cols-3">
    {[
      ['3 min', 'Pitch', 'Qué resuelve y en qué track compite'],
      ['3 min', 'Preguntas', 'El jurado defiende la rúbrica'],
      ['1', 'Demo en vivo', 'El flujo se recorre entero, sin video'],
    ].map(([value, title, detail]) => (
      <div key={title} className="flex min-h-48 flex-col items-center justify-center bg-[var(--screen-dim)] p-6 text-center">
        <div className="font-mono text-[clamp(1.5rem,3.5vw,2.5rem)] font-semibold leading-none tracking-[-0.05em] text-[var(--bright)]">{value}</div>
        <div className="mt-4 font-mono text-sm font-semibold uppercase tracking-[0.12em] text-[var(--text)]">{title}</div>
        <div className="mt-3 font-mono text-xs leading-relaxed text-[var(--text-dim)]">{detail}</div>
      </div>
    ))}
  </div>

  <div className="grid gap-3 md:grid-cols-3">
    {[
      ['Primer lugar', '$1,000 USD'],
      ['Segundo lugar', '$500 USD'],
      ['Tercer lugar', 'Créditos de partners'],
    ].map(([place, amount]) => (
      <div key={place} className="flex flex-col gap-2 bg-[var(--screen-dim)] p-5">
        <span className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-[var(--text-dim)]">{place}</span>
        <span className="font-mono text-lg font-semibold tracking-[-0.04em] text-[var(--text)] md:text-2xl">{amount}</span>
      </div>
    ))}
  </div>
</div>
