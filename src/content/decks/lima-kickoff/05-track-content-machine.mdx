---
title: Content Machine
---

<div className="flex min-h-[64svh] flex-col justify-center gap-9">
  <div className="flex flex-col gap-3">
    <p className="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-[var(--bright)]">Track 01 · ≈33 cupos</p>
    <h1 className="font-mono text-[clamp(2rem,5.5vw,4.25rem)] font-semibold uppercase leading-[0.95] tracking-[-0.06em] text-[var(--text)]">Content Machine</h1>
    <p className="font-mono text-xs uppercase tracking-[0.12em] text-[var(--text-dim)] md:text-sm">Crear · publicar · distribuir</p>
  </div>

  <div className="grid gap-8 md:grid-cols-2 md:gap-12">
    <div className="flex flex-col gap-4">
      <h2 className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-[var(--text-dim)]">Qué esperamos</h2>
      <p className="font-mono text-sm leading-relaxed text-[var(--text)] md:text-base">Construye herramientas que creen, editen, publiquen o distribuyan contenido — pensadas como producto real, no como demo. Que alguien las use y funcionen solas.</p>
      <p className="border-l-2 border-[var(--bright)] pl-4 font-mono text-sm leading-relaxed text-[var(--text-dim)]">Así crecen los productos hoy. El reto está en que funcione de verdad y a escala, no solo en la demo.</p>
    </div>
    <div className="flex flex-col gap-4">
      <h2 className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-[var(--text-dim)]">Ideas para arrancar</h2>
      <div className="flex flex-col gap-2">
        {[
          'Un asistente que convierte tu trabajo en contenido y lo publica solo',
          'Long-form → clips, eligiendo los mejores momentos y no cortes al azar',
          'Una herramienta que reutiliza tu contenido viejo de forma inteligente',
          'Publicar en varias plataformas a la vez, sin que se rompa',
          'No solo generar contenido: medir si de verdad conecta'
        ].map((idea) => (
          <div key={idea} className="bg-[var(--screen-dim)] px-4 py-3 font-mono text-sm leading-snug text-[var(--text)]">{idea}</div>
        ))}
      </div>
    </div>
  </div>
</div>
